# Core runtime — version ranges verified to build, validate, and serve the
# dashboard end to end. Lower bounds are the minimum tested; upper bounds guard
# against the next major release changing an API the project relies on.
pandas>=2.0,<4.0
numpy>=1.24,<3.0
scikit-learn>=1.3,<2.0
pulp>=2.7,<4.0          # PULP_CBC_CMD still ships through the 3.x line; optimize_roster falls back to COIN_CMD if it is ever removed
streamlit>=1.30,<2.0
altair>=5.0,<7.0
openpyxl>=3.1,<4.0
 
# Test suite
pytest>=7.4,<10.0
 
# Optional — per-player SHAP explanations on the dashboard. The app and
# models.explain_forecast both degrade gracefully (no explanation panel) when
# this is absent, so it is not required to run, validate, or test the project.
# Install with:  pip install "shap>=0.44"
# shap>=0.44,<1.0
 
